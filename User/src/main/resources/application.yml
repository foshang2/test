server:
  prot: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ShangTing?serverTimezone=Asia/Shanghai
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml  # XML映射文件路径
#  type-aliases-package: entity  # 实体类包路径
#  configuration:
#    map-underscore-to-camel-case: true  # 开启驼峰命名转换
#  log-impl: logging.stdout.StdOutImpl  # 日志实现

logging:
  level:
    org.mybatis: DEBUG
